struct obj (
    var id: i64
)

struct pi (
    var a: i64
    var b: i64
)

struct stat (
    struct pi (
        var a: i64*
    )
    struct g_obj (
        var origin: pi
        var root: obj 
    )
)