services:
  lkjagent:
    build: .
    image: lkjagent:latest
    container_name: lkjagent
    working_dir: /app
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
    restart: unless-stopped
